{
  "name": "sales-os-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Sales OS backend — webhook listener + daily lead-research cron writing into the second-brain",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "apify-client": "^2.9.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "node-cron": "^3.0.3"
  }
}
